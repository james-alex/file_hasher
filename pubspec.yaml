name: file_hasher
description: A utility for hashing one or more files with the XXH3 hashing algorithm.
version: 0.1.0
repository: https://github.com/james-alex/file_hasher

environment:
  sdk: '>=2.17.0 <3.0.0'

dependencies:
  xxh3: ^1.0.1

dev_dependencies:
  lints: ^2.0.0
  test: ^1.16.0
